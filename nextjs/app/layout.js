import "../styles/globals.css";
export const metadata = {
  title: "Sobre nós",
};
export default function RootLayout({ children }) {
  return (
    <html>
      <body>{children}</body>
    </html>
);
}
